# Task 02 — Matches and Attendance

Implement:

- Match;
- MatchParticipant;
- create match;
- list upcoming/recent matches;
- match detail;
- authenticated member updates own attendance;
- attendance states GOING, NOT_GOING, UNDECIDED;
- owner/treasurer locks participants;
- locked participants cannot be changed without an explicit authorized reopen action, if reopen is implemented;
- migrations and tests;
- mobile screens only if the repository contains the mobile app.

Enforce:

- team membership;
- resource ID + team ID lookup;
- role permissions;
- one participant row per match and team member;
- same-team invariants.

Do not implement cost splitting in this task.
